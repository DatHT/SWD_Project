CREATE DEFINER=`root`@`localhost` PROCEDURE `GetFoodBaseOnUser`(in user varchar(255), in member int,in ignorenumber int)

BEGIN
	
select FoodName, Tutorial, LinkImage, MaterialInfo, Description from iCookDB.tbl_food where  User=user order by VisitNum DESC limit member offset ignorenumber; 

END